# 鼓手陪练设计

本文描述 `REQUIREMENTS.md` 中“伴奏小能手”的首版技术设计。

## 设计原则

- 单机、单用户、本地优先。
- 使用一个 Python Web 服务和一个 Web 前端，不采用微服务。
- 媒体、模型和分析结果不上传第三方服务。
- 模型质量优先于分析速度。
- 鼓谱、drumless 和原音频彼此独立，局部失败不阻塞其他功能。
- 模型、节拍跟踪和谱面渲染均通过稳定的内部数据格式解耦。
- 次要实现选择由开发过程决定，不作为产品配置暴露。

## 总体架构

```text
Chrome / Chromium
  ├── React + TypeScript 单页应用
  ├── HTML Audio 播放
  └── VexFlow SVG 鼓谱
            │
            │ REST + SSE + HTTP Range
            ▼
Python / FastAPI
  ├── arecord 系统音频录制
  ├── mpv 服务端音频播放与 Unix socket 控制
  ├── 曲库与文件管理
  ├── SQLite metadata
  ├── 串行任务队列
  ├── 音频 Range 响应
  └── 分析任务执行器
            │
            ▼
独立分析子进程
  ├── FFmpeg
  ├── StemSeparator
  ├── BeatTracker
  └── DrumTranscriber
```

正式运行时，FastAPI 同时提供 API、媒体文件和编译后的前端静态文件。前后端同源，只暴露一个端口。电脑通过 `localhost` 使用，手机通过电脑的局域网地址使用。

不实现登录、权限、来源检查或多用户隔离。手机只读是前端产品行为，不是后端权限机制。

## 运行配置

配置文件固定为：

```text
~/.config/drummer_buddy/config.yaml
```

至少记录：

- 用户选择的曲库根目录。
- Web 服务监听地址和端口。
- 默认录音目录和本机 ALSA/Pulse monitor 名称。
- 模型及运行参数。
- 默认 drumless 输出格式。

SQLite 和媒体文件位于曲库目录中，不放入配置目录。

## 曲库布局

```text
<library>/
├── drummer_buddy.sqlite3
├── .incoming/
└── songs/
    └── <song-uuid>/
        ├── source/
        │   └── <original-filename>
        ├── audio/
        │   ├── original.flac
        │   └── drumless.flac
        ├── cache/
        │   ├── analysis.wav
        │   └── drums.flac
        ├── scores/
        │   ├── generated-v1.json
        │   └── edited-v1.json
        └── logs/
```

歌曲目录使用 UUID，不使用歌曲名或艺术家，避免重命名和同名冲突。

所有数据库路径均相对于曲库根目录保存，使整个曲库可以整体移动。音频不存入 SQLite BLOB。完整鼓谱和日志使用普通文件，SQLite 仅保存索引、版本和状态。

## 数据库职责

核心数据包括：

- `songs`：UUID、歌曲名、艺术家、本地内容 hash 和归档状态。
- `media_assets`：源文件、原始音频、drumless 和缓存文件。
- `score_versions`：生成或编辑版本及当前启用版本。
- `analysis_jobs`：任务类型、状态、进度、时间和错误。
- `model_runs`：模型名称、版本、参数和输出版本。

任务日志保存为文本文件，数据库只保存路径和摘要。

## 音源加入曲库

首版每次加入一首歌曲。本地文件流程：

1. 用户在桌面端直接输入电脑上的绝对文件路径。
2. 后端检查路径存在、是普通文件、可读且格式受支持。
3. 后端将文件复制到 `.incoming/`，同时计算 SHA-256 和复制进度。
4. 根据内容 hash 检查重复文件。
5. 重复的活动歌曲拒绝导入；已归档歌曲提示恢复。
6. 新文件原子移动到 UUID 歌曲目录。
7. 用户填写歌曲名和艺术家。
8. FFmpeg 创建内部标准音频。

不提供浏览器文件上传、文件选择器、目录扫描或批量导入。

系统音频录制流程：

1. 用户填写录音目录和歌名后启动录音。
2. 后端以参数数组启动 `arecord -D <configured-monitor> -f cd <path>`，不执行 shell 拼接。
3. 用户在其他页面播放音源，结束后停止录音。
4. “Clean recorded”验证 WAV 非空，通过现有本地导入流程复制进曲库，并保留原始录音。

monitor 名称只来自本机配置。一次只允许一个录音进程；服务关闭时向录音进程发送 SIGINT，使 WAV 头正确落盘。

## 内部音频格式

- 永久保留用户导入的源文件。
- 内部原音频统一为 44.1 kHz、双声道 FLAC。
- 不做响度标准化。
- 模型临时输入可使用 WAV。
- drumless 与内部原音频使用相同采样率、声道数和采样数。
- 分离后通过裁剪或补零保证两个版本时间轴完全一致。
- 用户导出 drumless 时选择 WAV 或 FLAC。

MP4 只提取音轨，不处理或输出视频。

## 曲目与资产状态

不设置单一的“歌曲已分析”状态。各资产独立记录状态：

```text
原始音源：ready / unavailable / failed
drumless：missing / queued / running / ready / failed
鼓谱：missing / queued / running / ready / failed
```

- 本地原音频 ready 后即可播放。
- 曲目无须 drumless 或鼓谱即可播放。
- drumless 缺失或失败不影响原曲播放和鼓谱分析。
- 鼓谱缺失或失败不影响音频播放和 drumless。
- 重新分析期间继续保留旧的成功版本。
- 新结果成功生成后才允许成为当前版本。

## 后台任务

Web 请求只创建任务，不直接运行模型。一个任务执行器严格串行运行 GPU 任务。

任务状态：

```text
queued / running / cancelling / cancelled / succeeded / failed / interrupted
```

执行规则：

- 每个分析任务运行在独立子进程中。
- 子进程通过 JSON Lines 输出阶段、进度和日志。
- 日志同时写入歌曲日志目录，并通过 SSE 推送给前端。
- 页面刷新后先通过 REST 读取任务状态，再重新连接 SSE。
- 取消任务时终止整个子进程，并丢弃临时结果。
- 输出先写临时目录，全部成功后根据 manifest 原子入库。
- Web 服务启动时将残留的 `running` 任务标记为 `interrupted`，不自动续跑。

不使用 Redis、Celery 或分布式队列。

## 分析进程接口

分析子进程使用文件和 JSON 作为边界，不要求 Web 服务直接导入模型包。

```text
输入：音频路径、任务参数、临时输出目录
进度：stdout JSON Lines
结果：manifest.json + 音频/节拍/鼓点文件
```

进度示例：

```json
{"stage":"separation","progress":0.42,"message":"processing segment 18/43"}
```

模型初期可以共用一个 Python 环境。如果出现真实依赖冲突，可让不同适配器使用独立环境，Web 和数据库接口无需变化。

## 分析流水线

```text
FFmpeg 解码与标准化
        ↓
鼓轨分离
        ↓
鼓点事件识别
        ↓
原混音节拍与重拍分析
        ↓
事件量化与声部分配
        ↓
内部鼓谱 JSON
```

首轮候选方案：

- `StemSeparator`：Demucs `htdemucs_ft`。
- `BeatTracker`：Beat This!。
- `DrumTranscriber`：ADTOF PyTorch 模型。

drumless 由非鼓 stem 重新混合得到。鼓谱分析可复用缓存的独立鼓轨，但“生成鼓谱”和“生成 drumless”仍作为两个独立用户任务。

所有模型均通过以下抽象接口接入：

```text
StemSeparator
BeatTracker
DrumTranscriber
```

模型选择必须先用用户实际歌曲验证。自动拍号和复杂拍号变化为尽力识别，用户编辑作为兜底，不作为所有歌曲都必须准确的硬性验收条件。

## 时间模型

系统同时保存真实音频时间和音乐时间。

### 逐拍时间锚点

不把 BPM 作为唯一事实来源，而是保存每一拍的实际时间：

```json
{
  "beats": [
    {"timeSec": 12.10, "measure": 7, "beat": 1},
    {"timeSec": 12.61, "measure": 7, "beat": 2}
  ],
  "timeSignatures": [
    {"measure": 1, "numerator": 4, "denominator": 4}
  ]
}
```

局部 BPM 根据相邻拍点计算。逐拍锚点可表达现场演奏中的渐快、渐慢和轻微漂移。

### 音乐 ticks

内部固定使用 960 PPQ：

- 四分音符：960 ticks。
- 八分音符：480 ticks。
- 十六分音符：240 ticks。
- 八分三连音：320 ticks。
- 十六分三连音：160 ticks。

逐拍锚点负责在 ticks 和实际秒数之间映射。

### 鼓点事件

```json
{
  "id": "event-uuid",
  "instrument": "snare",
  "detectedTimeSec": 12.864,
  "onsetTick": 26880,
  "durationTicks": 480,
  "confidence": 0.94
}
```

- `detectedTimeSec` 保留模型检测到的原始声音位置。
- `onsetTick` 是量化后的谱面位置。
- `durationTicks` 是乐谱时值，不表示鼓声物理衰减时间。
- 调整节拍图后可以重新量化，同时不丢失原始检测结果。

## 鼓件模型

内部定义稳定的语义鼓件 ID，不直接保存模型标签：

```text
kick
snare
side_stick
tom_low
tom_mid
tom_high
hihat_closed
hihat_open
hihat_pedal
crash
ride
china
splash
```

每个鼓件映射五线谱位置、音符头、符干规则和可选的 General MIDI percussion 编号。模型适配器负责将模型输出映射到内部 ID。

自动量化器负责：

- 合并同时发生的多个鼓件。
- 生成常见时值、休止符和连桁。
- 将镲、踩镲、军鼓和通鼓分配到上层声部。
- 将底鼓和踩镲踏板分配到下层声部。

## 鼓谱版本

每次模型分析创建不可修改的生成版本。用户第一次编辑时，从当前版本派生独立编辑版本。

- `generated`：模型生成。
- `edited`：用户编辑。

重新分析创建新生成版本，不覆盖旧版本，也不尝试自动合并旧编辑。用户选择当前使用的版本。旧版本继续保留，直到用户明确隐藏或清理。

鼓谱 JSON 使用 revision 编号。保存时先写临时文件再原子替换，避免手机或播放器读取半写入文件。

## 谱面渲染与分页

内部鼓谱 JSON 是唯一事实来源，VexFlow 只负责渲染：

```text
鼓谱 JSON
    ↓
自有排版层：小节、行和页
    ↓
VexFlow SVG
```

- 页面只在小节边界分页。
- 桌面每页显示多行。
- 手机每页显示一至两行。
- 预先渲染当前页和相邻页，降低翻页延迟。
- 音符 SVG 元素绑定事件 ID。
- 播放高亮只修改 CSS，不重新渲染谱面。
- 编辑、拍号变化或布局宽度变化时才重新排版受影响页面。

不使用瀑布流，不压缩重复小节。

## 播放与高亮

播放器可使用 HTML `<audio>` 在当前浏览设备出声，或通过后端控制单个 `mpv` 进程在服务器电脑出声。本地曲目在开始练习前选择原曲或 drumless，播放过程中不能切换音源。输出设备切换时传递播放位置、暂停状态和整曲循环状态。

本地媒体的播放循环以 `audio.currentTime` 为唯一权威时钟：

1. `requestAnimationFrame` 每帧读取当前时间。
2. 加上用户设置的全局同步偏移。
3. 使用二分查找定位当前鼓点和页面。
4. 高亮同一时刻发生的全部鼓件。
5. 根据逐拍锚点插值移动播放位置线。
6. 到达下一页时间范围后自动翻页。

不为每个音符创建 `setTimeout`。暂停、跳转和浏览器卡顿后均从音频时钟重新计算状态。

后端媒体响应支持 HTTP Range，使跳转无需完整读取音频。

### 预备拍

- 仅首次开始播放时提供预备拍。
- 默认一小节，可切换为两小节。
- 使用歌曲开头的节拍图确定速度和拍号。
- 暂停恢复或跳转后继续不再次播放预备拍。

## 鼓谱编辑器

首版只提供桌面轻量编辑：

- 点击音符选择。
- 点击空白节拍位置添加音符。
- 水平拖动并吸附到支持的节拍网格。
- 使用侧边栏修改鼓件类型和时值。
- 删除选中音符。
- 修改 BPM、逐拍锚点、拍号、小节线和全局偏移。
- 每次编辑生成 command，支持撤销。
- 停止操作约 500 ms 后自动保存。

鼓件类型不通过垂直拖动猜测。首版不模拟专业制谱软件的完整输入方式。

## 前端页面

- `/library`：曲库、本地文件路径导入和归档入口。
- `/utils`：系统录音及后续音频编辑工具。
- `/songs/:id`：metadata、资产状态、分析任务和版本。
- `/songs/:id/edit`：桌面鼓谱编辑。
- `/practice/:id`：练习播放。
- `/tasks`：任务队列、日志和取消。
- `/archive`：归档列表和恢复。

同一个 React 应用通过响应式布局适配桌面和手机。手机隐藏导入、分析、归档和编辑功能。

不实现 PWA、离线缓存、SSR 或单独的手机应用。

## 练习页

练习页保持简单：

- 本地曲目进入播放前选择原曲或 drumless。
- 开始、暂停和任意跳转。
- 有鼓谱的本地曲目显示传统分页鼓谱、当前音符高亮、播放位置线和自动翻页。
- 有节拍分析结果的本地曲目提供一小节或两小节预备拍设置。
- 本地曲目提供全局延迟偏移校准。

电脑浏览时由电脑播放音频；手机浏览时由手机播放音频。每台设备使用自己的浏览器音频时钟，不实现跨设备同步播放。

## 不包含的旁支

- PDF 或图片鼓谱导入与 OMR。
- MusicXML、MIDI 或其他外部鼓谱导入。
- 播放过程中切换原曲和 drumless。
- 公网访问、多用户和权限系统。
- PDF/MusicXML 导出和打印。

## 实施顺序

### 0. 分析效果验证

使用 5–10 首用户实际歌曲验证：

- Demucs drumless 和独立鼓轨听感。
- ADTOF 基础鼓件识别。
- Beat This! 拍点、重拍和速度变化。
- 鼓点量化和静态 VexFlow 鼓谱。
- 处理耗时、显存峰值和失败模式。

这一阶段不实现完整曲库、编辑器、队列或手机界面。

### 1. 本地应用骨架

实现配置、SQLite、曲库、系统录音、本地路径导入、metadata、基础播放和归档。

### 2. 后台任务系统

实现串行队列、分析子进程、取消、进度、日志和中断状态。

### 3. drumless

实现标准化、分离、缓存、试听和 WAV/FLAC 输出。

### 4. 鼓谱与播放器

实现内部 JSON、VexFlow 分页、同步播放、高亮、跳转和预备拍。

### 5. 编辑与版本

实现自动保存、撤销、节拍图校正、鼓点编辑和版本切换。

### 6. 手机练习界面

实现响应式分页、手机本机播放和只读产品界面。

## 主要风险

1. 自动鼓点识别质量是项目的核心风险，必须在完整应用开发前验证。
2. 自动拍号和复杂拍号变化无法保证对所有歌曲准确，必须允许手动修正。
3. 分离模型会产生串音和伪影，原曲必须始终可用。
4. 蓝牙和物理输出设备可能产生额外延迟，需要全局偏移校准。
5. 不同模型的 Python 和 CUDA 依赖可能冲突，因此保留进程隔离边界。
