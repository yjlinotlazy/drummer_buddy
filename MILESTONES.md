# 鼓手陪练实施里程碑

本文把 `REQUIREMENTS.md` 和 `DESIGN.md` 拆成可独立验收的增量。每个里程碑完成后，已有功能都应保持可用。

当前状态：

| 里程碑 | 状态 |
| --- | --- |
| M0 | 实现完成 |
| M1 | 实现完成；等待真实浏览器和设备验收 |
| M2 | 实现完成 |
| M3 | 实现完成；等待用户歌曲的听感验收 |
| M4 | 核心分析、基础谱面和同步高亮完成；分页、预备拍和真实歌曲验收待完成 |
| M5–M6 | 未开始 |

当前版本相较于最初 MVP 还包含：独立的“练习”标签页与月历打卡、CSV 持久化的练习记录和项目清单、文件名自动推断曲名、浏览器/服务器播放音量控制，以及手机端默认使用本机播放。

## M0：需求与技术基线

目标：建立可执行的产品边界和工程骨架。

- 明确本地文件与系统音频录音两类输入。
- 外部网页音频只通过用户主动启动的 `arecord` monitor 录制。
- 固定 FastAPI、React、TypeScript、SQLite 的单体架构。
- 建立后端、前端、测试、配置和开发命令。

验收：新环境可按 README 启动后端和前端；基础测试可运行。

## M1：可播放曲库 MVP

目标：不依赖任何模型即可把应用当作伴奏曲库使用。

- 首次启动创建配置和 SQLite 数据库。
- 从绝对路径导入 MP3、WAV、FLAC 或 MP4，并复制进曲库。
- 导入时计算 SHA-256，拒绝活动曲目的重复内容；已归档重复项可恢复。
- 独立 Utils 页中可编辑录音目录和歌名，启动/停止录音，并通过“Clean recorded”加入曲库；后续编辑工具也放在此页。
- 浏览曲库、查看曲目、编辑歌名和艺术家、归档与恢复。
- 本地原音频支持播放、暂停、跳转和整曲循环，并可切换当前设备或服务器电脑输出。
- 桌面和手机均可浏览与播放；手机隐藏管理操作。

验收：导入一个本地文件并录制一段系统音频后，两者无需分析即可播放；重启应用后数据仍存在；归档与恢复有效。

## M2：后台任务基础设施

目标：为所有 GPU/媒体处理提供可靠的串行执行边界。

- SQLite 持久化任务及状态机。
- 严格串行的独立子进程执行器。
- JSON Lines 进度协议、文件日志和 SSE 实时更新。
- 排队和运行任务可取消；临时输出成功后原子入库。
- 服务重启后把残留运行任务标记为 `interrupted`。

验收：用模拟分析器验证排队、进度、刷新恢复、取消、失败和服务重启行为。

## M3：本地音频标准化与 drumless

目标：让本地曲目生成可练习的去鼓伴奏。

- FFmpeg 生成 44.1 kHz 双声道 FLAC 标准音频。
- 接入 Demucs `htdemucs_ft`，缓存独立鼓轨并重混非鼓 stem。
- 裁剪或补零，使原曲和 drumless 采样数一致。
- 保留旧成功版本，失败不影响原曲播放。
- 支持 WAV 或 FLAC drumless 输出。

验收：在 RTX 4060 上用用户提供的歌曲验证听感、时长一致性、显存峰值、进度和失败恢复。

## M4：鼓谱生成与同步练习

目标：从本地曲目生成可同步练习的传统鼓谱。

- 接入 Beat This! 和 ADTOF，并记录模型版本与参数。
- 生成逐拍时间锚点、拍号、960 PPQ 鼓点事件和内部谱面 JSON。
- 量化基础时值、三连音、休止符和上下声部。
- VexFlow SVG 分页渲染，支持响应式行数。
- 本地音频播放、高亮、位置线、自动翻页、跳转和预备拍。
- 同步偏移可调，本地播放同步目标约 50 ms。

验收：用 5–10 首用户实际歌曲评估识别质量，并记录不能自动可靠处理的情况。

## M5：鼓谱编辑与版本

目标：用户可以修正模型错误而不破坏生成结果。

- 生成版本不可变；编辑时派生独立版本。
- 添加、选择、删除、水平移动鼓点并修改鼓件和时值。
- 编辑拍点、拍号、小节线和全局偏移。
- command-based 撤销、约 500 ms 自动保存和原子替换。
- 重新分析不覆盖旧版本，由用户选择当前版本。

验收：刷新和重启后编辑仍存在；撤销、版本切换及重新分析均不丢数据。

## M6：手机体验与发布收尾

目标：把本机开发项目整理成稳定的局域网应用。

- 手机端只保留浏览和播放界面。
- 验证局域网地址、媒体 Range、录音流程和常见屏幕尺寸。
- 完善错误提示、空状态、任务日志和恢复路径。
- 提供安装、模型下载、启动、升级和备份说明。
- 建立端到端冒烟测试和发布检查清单。

验收：电脑启动一次服务后，电脑和同网手机可完成各自支持的主流程。

## 外部依赖点

以下事项不阻塞 M0–M2，但会影响后续验收：

- M3–M4 需要 NVIDIA/CUDA 环境和 5–10 首有代表性的用户歌曲。
- 系统录音依赖本机 PipeWire/PulseAudio monitor 名称，硬件相关值只放在本地配置中。
- 模型选择必须以实际听感和识别结果为准，不能只凭公开 benchmark 决定。
