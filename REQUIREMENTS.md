# 鼓手陪练

一系列方便鼓手训练和娱乐的小工具。

## 伴奏小能手

### 目标

一个运行在用户电脑上的本地 Web 应用。基础功能是建立伴奏曲库并播放本地音频。用户也可在其他网页播放音源，由应用通过系统音频 monitor 录成 WAV，再加入曲库。本地曲目可以按需生成同步的传统鼓谱和去除鼓声的伴奏音频。本地媒体和分析数据不上传第三方服务。

应用通过电脑上的服务运行：

- 电脑使用 `localhost` 访问。
- 手机可通过同一局域网访问电脑上的服务，不支持公网访问。
- 单用户使用，不需要账户、登录或用户切换。
- 首版只保证最新版 Chrome/Chromium。

### 本地配置与曲库

- 配置文件位于 `~/.config/drummer_buddy/config.yaml`。
- 用户可自行选择曲库目录，目录位置记录在配置文件中。
- 曲库保存本地源文件副本、录音、提取音频、分析结果、drumless 音频、中间文件和任务日志。
- 曲目 metadata 至少包括可手动编辑的歌曲名和艺术家。
- 导入文件时复制到曲库，不依赖原文件路径。
- 通过文件内容识别重复文件并阻止重复导入。
- 如果重复文件对应已归档曲目，提示用户直接恢复该曲目。
- 移除曲目时只标记为归档，不移动或删除文件。
- 提供已归档列表，并允许恢复曲目。

### 支持的输入与输出

输入格式：

- 音频：MP3、WAV、FLAC。
- 视频：MP4。视频仅作为方便导入 MTV 等内容的容器；应用提取其音轨，不保存或输出处理后的视频。
- 系统录音：通过配置的 ALSA/Pulse monitor 使用 `arecord` 录制 44.1 kHz 双声道 WAV。

输出格式：

- drumless 音频支持 WAV 和 FLAC，由用户选择。
- 始终保留原始音频。即使 drumless 效果不好，用户仍可使用原曲练习。
- 首版不要求导出或打印鼓谱；未来可扩展 PDF、MusicXML 等格式。

### 导入和分析流程

1. 用户手动导入本地音频或视频文件，或者录制其他页面正在播放的系统音频。
2. 录音目录、文件名和 monitor 可配置；用户停止录音后通过“Clean recorded”加入曲库，原始录音保留。
3. 曲目加入后即可播放原始音源，不自动分析。
4. “生成鼓谱”和“生成 drumless”是两个独立任务，可分别运行、失败或重试。
5. 多首歌曲的任务进入队列，一次只运行一个任务，以控制 GPU 显存占用。
6. 排队中和运行中的任务均可取消。取消后保留日志，丢弃未完成结果。

录音加入曲库后与其他本地歌曲一致，可生成 drumless 或鼓谱。

分析任务要求：

- 针对当前配有 NVIDIA RTX 4060 的电脑优化，可以依赖 CUDA，不要求纯 CPU 设备有良好性能。
- 允许首次联网下载预训练模型；下载完成后可离线运行。
- 优先使用成熟的预训练模型。首版不要求本地训练，但未来允许用标注数据微调。
- 最终效果优先于分析速度，但分析过程必须持续提供进度和日志，不能无反馈地长时间运行。
- 浏览器刷新或关闭后，后端任务继续运行；重新打开页面可恢复任务状态、进度、错误和日志。
- 应用或电脑关闭后，未完成任务直接中断，不要求重启后自动续跑。
- 允许缓存鼓声分离轨等中间文件，以便重试和重新分析。

### 鼓谱生成要求

模型准确率优先，目标是多数常见歌曲生成后可直接练习，编辑器主要作为纠错保障。

- 必须识别常见基础鼓件，例如底鼓、军鼓、踩镲、通鼓和吊镲。
- 开闭镲、边击、鬼音和滚奏尽力识别，识别不完整可以接受。
- 不记录或显示力度。
- 支持常见流行、摇滚、爵士等风格。
- 重金属、硬核、复杂双踩等内容只要求大致还原，不追求完全准确。
- 必须分析随歌曲变化的 BPM、拍号、小节线和时间映射，而不是假定全曲固定 4/4 和固定 BPM。
- 至少正确表示四分、八分、十六分音符和三连音；更复杂的连音允许近似。
- 所有小节完整展开，不用反复记号压缩，以便谱面和播放时间一一对应。

### 鼓谱编辑

编辑功能只在桌面端提供，首版包括：

- 点击谱面添加音符并选择鼓件。
- 选择、删除和移动音符的时间位置。
- 调整 BPM、拍号、小节线和整段时间偏移。
- 自动保存修改，并支持撤销误操作。

模型生成结果和用户手动修改分开保存。重新分析时，由用户选择保留手动修改，或用新结果覆盖并重置修改。

首版不追求专业制谱软件的完整输入能力。

### 谱面显示

- 只采用传统五线鼓谱，不使用瀑布流。
- 采用传统分页布局，不连续滚动。
- 播放到页尾时自动翻页。
- 桌面版每页可显示多行；手机版每页只排一至两行，确保可读性。
- 播放时同时高亮当前音符，并显示随播放移动的位置线，使用户能预判下一拍。

### 练习播放

- 没有鼓谱或 drumless 的曲目仍可直接播放原始音源。
- 用户在开始练习前选择原曲或 drumless，播放过程中不能切换音源。
- 原曲和 drumless 必须使用同一时间轴，与同一份鼓谱保持同步。
- 首版支持开始、暂停和任意位置跳转。
- 首版不要求变速播放、小节循环或常驻节拍器。
- 有节拍分析结果时，第一次点击开始播放预备拍，默认一小节，用户可切换为两小节。
- 暂停后继续或跳转后继续时不重新播放预备拍。
- 音频由当前用于练习的设备播放：电脑浏览时电脑出声，手机浏览时手机出声。谱面和音频使用同一浏览器播放时钟。
- 播放器可在“This device”和“Server”之间切换输出；Server 模式通过服务端 `mpv` 从运行 Drummer Buddy 的电脑出声，同一时间只维护一个服务端播放器。
- 切换输出时保留当前播放位置、暂停状态和整曲循环状态。
- 本地音频与谱面的同步误差目标不超过约 50 ms。
- 提供全局延迟偏移校准，用于补偿模型对齐误差、物理音响或蓝牙设备延迟。

### 手机端

手机端用于练习，只提供：

- 浏览和选择曲目。
- 查看分页鼓谱。
- 控制开始、暂停和跳转。
- 在手机本机播放练习音频。

手机端不提供导入、分析或鼓谱编辑功能。

### 首版暂不包含

- 公网访问和部署。
- 多用户、账户和权限系统。
- Safari 等非 Chrome/Chromium 浏览器适配。
- 处理后的视频输出。
- 鼓谱导出和打印。
- 专业制谱软件级别的编辑功能。
- 变速播放、小节循环和常驻节拍器。
- 应用重启后的分析任务断点续跑。

## 练习追踪

专门占用一个标签页“练习”

 - 有个日历，每日可以打卡练了什么项目，可以有不止一个。
 - 提供几个初始练习，用户可以简单按+添加项目
 - 可以点击过去的日期补打卡
- 初始练习包括 `lifetime`、`paradiddle`、`playalong`。
- 练习记录保存为本机 CSV：`/home/yli/Dropbox/Music/DrumBuddyPractice/practice.csv`。
- 用户自定义的练习项目保存为同目录的 `items.csv`；应用重启或换浏览器后仍可恢复。
